<div class="container-fluid">
    <div id="PagePreloader" class="container PagePreloader">
        <center style="margin:10% 0 0 0">
            <div class="loader"></div>
            <br />
            Chargement en cours
        </center>
    </div>

    @{
/* public static*//*
string RemoveSpecialCharacters(string str)
{
    System.Text.StringBuilder sb = new System.Text.StringBuilder();
    foreach (char c in str)
    {
        if ((c >= '0' && c <= '9') || (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z') || c == '.' || c == '_')
        {
            sb.Append(c);
        }
    }
    return sb.ToString();
}


 .replace(/[éèëê]/g, "e")
.replace(/[à]/g,"a")
.replace(/[ç]/g,"c")
.replace(/[^a-zA-Z0-9 ]/g, "")
.replaceAll("  ", " ").replaceAll("  ", " ").replaceAll("  ", " ").replaceAll("  ", " ")
.replaceAll(" ", "_");

string s = "aézèeërêtàyçu demande d'ahcta 253    qsd     é\" )àç_è-( hsdlhk,n  khjbkjb65312                         r";
s = RemoveSpecialCharacters(
        s
        .Replace("  ", " ").Replace("  ", " ").Replace("  ", " ").Replace("  ", " ")
        .Replace(" ", "_")
    );
<h1>@s</h1>*/
    }


    <div id="viewcontainer">

    </div>
</div>